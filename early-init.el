(setenv "LSP_USE_PLISTS" "true")

(setq default-frame-alist
       '((height . 0.86)
         (width . 0.48)
         (left + 10)
         (top + 30)
	 (vertical-scroll-bars . nil)
	 (tool-bar-lines . 0)))
